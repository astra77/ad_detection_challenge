name: hello-world
on: [push]
jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
       - name: Hello world action
         env: 
           super_secret: ${{ secrets.PAGE_CR }}
       - run: echo "Hello world!";  
  
